# ============================================
# Discord Selfbot - Dynamic Channel Mirroring
# ============================================
# This bot automatically creates mirror channels and webhooks
# Supports both webhook mode and bot mode!

# ============================================
# REQUIRED SETTINGS
# ============================================

# Your Discord selfbot token (REQUIRED)
TOKEN=your_discord_token_here

# Target server ID where mirror channels will be created (REQUIRED)
# How to get: Enable Developer Mode → Right-click server → Copy ID
TARGET_GUILD_ID=your_target_guild_id_here

# ============================================
# CHANNEL SELECTION MODE (Choose ONE)
# ============================================

# OPTION 1: Individual Channel Monitoring (default)
# List specific channels to monitor
# How to get: Enable Developer Mode → Right-click channel → Copy ID
CHANNEL_1=123456789012345678
CHANNEL_2=234567890123456789
CHANNEL_3=345678901234567890
# Add more channels (supports unlimited):
# CHANNEL_4=456789012345678901
# CHANNEL_5=567890123456789012

# OPTION 2: Full Server Copy Mode (alternative)
# Monitor ALL channels in a source server
# Enable this mode by setting FULL_SERVER_COPY=true
FULL_SERVER_COPY=false
SOURCE_GUILD_ID=
# When enabled, ignores CHANNEL_X settings and copies entire server

# ============================================
# FORWARDING MODE
# ============================================

# Use bot mode instead of webhooks (optional)
# Bot mode allows replying and editing messages
# Webhook mode is simpler but can't reply/edit
USE_BOT_MODE=false

# Bot token (REQUIRED if USE_BOT_MODE=true)
# This should be a separate bot account token
# How to get: https://discord.com/developers/applications
BOT_TOKEN=

# ============================================
# OPTIONAL SETTINGS
# ============================================

# Bot username displayed in webhook messages (webhook mode only)
BOT_USERNAME=Mirror Bot

# Category ID to create all mirror channels under (optional)
# Leave empty to create channels at server top level
# How to get: Enable Developer Mode → Right-click category → Copy ID
TARGET_CATEGORY_ID=

# Copy source channel's category structure (optional)
# If true: Creates matching categories from source channels
# If false: Uses TARGET_CATEGORY_ID or creates at top level
COPY_CATEGORY_STRUCTURE=false

# ============================================
# NOTES
# ============================================
# - Webhooks/channels are created automatically (no manual setup!)
# - Bot mode: More features (replies, edits) but needs bot token
# - Webhook mode: Simpler, just needs selfbot token
# - Full server mode: Copies ALL channels from a server
# - Webhook URLs are stored in webhooks.json (auto-generated)
# - Requires "Manage Channels" and "Manage Webhooks" permissions in target server
# - Bot account must be a member of the target server